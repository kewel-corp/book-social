# 医療診断支援AI 四分冊シリーズ ― ライブラリのバージョン固定
#
# 実装編「pip・requirements.txt・uv ― パッケージを入れる／固定する」の節に載せた、
# 3次元CTセグメンテーションを想定した構成をそのまま置いています。
#
# バージョン指定の作法（実装編の同じ節）：
#   `==` は「この版でなければ不可」という厳密固定。再現性は最高だが、他ライブラリとぶつかりやすい。
#   `>=` は緩く衝突しにくい反面、あとで動作が変わる恐れがある。
#   再現性が問われる医療AIでは、少なくとも本番・論文用の環境は `==` で固定する。
#
# Colab には多くが最初から入っています。入門編のノートブックを Colab で動かすだけなら、
# このファイルを丸ごと入れる必要はありません。

torch==2.4.0
torchvision==0.19.0
monai==1.3.2          # 医療画像向けディープラーニング
nibabel==5.2.1        # NIfTI(.nii.gz)の読み書き
pydicom==2.4.4        # DICOMの読み書き
numpy==1.26.4
scikit-image==0.24.0
matplotlib==3.9.2

# --- 本文の一覧に加えて、評価・後処理のコードが使うもの（2026-09-12） ---
scipy==1.15.3                 # 病変対応づけ（最大マッチング）、距離変換、中心線の近傍探索
scikit-learn==1.7.2           # 感度・AP・AUC・較正曲線など
connected-components-3d       # import 名は cc3d。連結成分ラベリング（版は配布元の最新を確認）
