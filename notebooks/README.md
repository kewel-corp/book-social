# 社会実装編（『医療診断支援AIの社会実装』）のノートブック

第1〜37章。ファイル名は `chNN_*.ipynb`（`NN` は章番号）です。コードブロックの無い章は作っていません。

紙面のコードは「読んで理解するため」のもの、ここのノートブックは「動かすため」のものです。
PDFからコードをコピーすると、紙面の都合による折り返しや記号の変換で崩れることがあります。

**このフォルダに患者データを置かないでください。** リポジトリ直下の `.gitignore` で
DICOM・NIfTI・配列形式を機械的に締め出していますが、最後に効くのは運用です。
